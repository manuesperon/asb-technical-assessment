import React, { FC } from "react";
import * as styles from "./CardRegistrationForm.module.scss";

export const CardRegistrationForm: FC = () => {
  return (
    <div className={styles.cardRegistrationForm}>Card Registration Form</div>
  );
};
